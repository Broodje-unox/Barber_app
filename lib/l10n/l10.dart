import 'package:flutter/material.dart';

// supported languages
// english is the primary language, so we put description into it
class L10n {
  static final all = [const Locale('en'), const Locale('ja')];
}
